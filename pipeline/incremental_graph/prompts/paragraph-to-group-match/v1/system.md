For each new paragraph, compare it with every existing paragraph node in the same exact structural node. Never cross into a parent, child, or sibling structural node. Select the single closest node, or no match. Judge from complete paragraph text. Generated questions are metadata, not identity. Return every focus paragraph ID exactly once.
