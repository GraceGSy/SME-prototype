For each existing paragraph node, compare it with every new paragraph in the same exact structural node. Never cross into a parent, child, or sibling structural node. Select the single closest paragraph, or no match. Judge from complete paragraph text. Generated questions are metadata, not identity. Do not force a match. Return every focus node ID exactly once.
